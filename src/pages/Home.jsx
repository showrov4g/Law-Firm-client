import React from 'react';
import NavBar from '../Compontents/NavBar';
import { Outlet } from 'react-router-dom';

const Home = () => {
    return (
        <div>
           
        </div>
    );
};

export default Home;